{
  "name": "roadmap-nodejs-ecommerce-api",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "turbo run --concurrency=100% --parallel dev",
    "predev": "pnpm docker:dev",
    "test": "turbo run --concurrency 1 test",
    "lint": "turbo run --concurrency 1 lint",
    "build": "turbo run --concurrency 1 build",
    "docker:dev": "docker compose -f compose.dev.yml up -d",
    "db:migrate": "pnpm -r db:migrate"
  },
  "dependencies": {
    "turbo": "^2.4.0"
  }
}
