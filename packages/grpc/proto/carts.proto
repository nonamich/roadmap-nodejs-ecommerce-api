syntax = "proto3";

package carts;

import "google/protobuf/empty.proto";

message GetCartRequest {
  string userId = 1;
}

message AddToCartRequest {
  string productId = 1;
  int32 quantity = 2;
  string userId = 3;
}

message RemoveFromCartRequest {
  string userId = 1;
  string productId = 2;
}

message RemoveCartRequest {
  string userId = 1;
}

service CartsService {
  rpc AddToCart(AddToCartRequest) returns (CartResponse);
  rpc GetCart(GetCartRequest) returns (CartResponse);
  rpc RemoveFromCart(RemoveFromCartRequest) returns (CartResponse);
  rpc RemoveCart(RemoveCartRequest) returns (google.protobuf.Empty);
}

message CartItemResponse {
  int32 quantity = 1;
  string productId = 2;
  double price = 3;
}

message CartResponse {
  repeated CartItemResponse items = 1;
  int32 totalQuantity = 2;
  double totalPrice = 3;
}
